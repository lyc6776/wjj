#!/bin/bash

# 这里填那4个必填的参数，以后改直接改这里就行
P1="参数1"
P2="参数2"
P3="参数3"
P4="参数4"

# 后面是默认参数
P5="${P5:-64}"
P6="${P6:-65555}"
P7="${P7:-16384b}"
P8="${P8:-131072b}"
P9="${P9:-proxy.txt}"

# 直接调用 ./http2flood3.0 并把所有参数传过去
./http2flood3.0 "$P1" "$P2" "$P3" "$P4" "$P5" "$P6" "$P7" "$P8" "$P9"
